<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <h1>You Must <a href="/">Log In</a> to View Your Balance</h1>
    <%if is_logged_in?%>
      <h1>Welcome <%=curent_user(params).username%></h1>
      <h2>Balance <%=curent_user(params).balance%></h2>
     <%else%>
       <a href='/'>Homepage</a>
    <%end%>
   
    <a href='/logout'>Log Out</a>
  </body>
</html>